<Window x:Class="ProyectoFinalUniversidad.CapaPresentacion.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProyectoFinalUniversidad.Views"
        mc:Ignorable="d"
        Title="Iniciar Sesión"
        Height="450"
        Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#800000">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Iniciar Sesión" Foreground="White" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,30"/>
            <TextBlock Text="Carnet de Identidad" Foreground="White" Margin="0,0,0,5"/>
            <TextBox x:Name="txtCi" Background="White" Margin="0,0,0,20"/>
            <TextBlock Text="Contraseña" Foreground="White" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPassword" Background="White" Margin="0,0,0,30"/>
            <Button Content="Ingresar" Click="BtnIngresar_Click" Background="White" Foreground="#800000" Padding="15,5" Margin="0,0,0,20"/>
            <TextBlock Text="¿No tienes cuenta? Regístrate" Foreground="White" HorizontalAlignment="Center" MouseDown="TxtRegistrarse_MouseDown" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
